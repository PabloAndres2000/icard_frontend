import React from 'react'
//Llamamos la ruta y la importamos en la vista de la app
import { Navigation } from "./routes"


export default function App() {
    // <Navigation> -> Llamamos el contenido que tiene nuestra navigation en nuestra app
    return ( <
        div >
        <
        Navigation / >
        <
        /div>

    );
}